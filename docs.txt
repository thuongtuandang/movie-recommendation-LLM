QDRANT SETUP

docker run -p 6333:6333 qdrant/qdrant

run movies_metadata.ipynb to create vector db, link to your vector db

http://localhost:6333/dashboard#/collections/books

START MODEL_SERVICE

uvicorn main:app --host 0.0.0.0 --port 8001

START BACKEND

python manage.py runserver

START FRONTEND

npm start

Without docker
client = QdrantClient(host='localhost', port=6333)

docker build -t movie-recommendation-ui .
docker run -d -p 3000:3000 movie-recommendation-ui

